# Automated verification report

**Executed:** 2026-09-02  
**Result:** **PASS for the automated P0 scope**  
**Manual boundary:** **REAL CAPELLA TEST: NOT EXECUTED**

This report records the final consolidated run. Fixture results are evidence for
the ReqPilot fixture adapter and UI only; they are not evidence that Eclipse
Capella 7.1.0 or Requirements Viewpoint 0.14.0 was executed.

## Release gates

| Gate | Result | Recorded outcome |
|---|---|---|
| `doctor` | PASS | Python, Node, StrictDoc, capellambse, configuration and repository paths accepted; missing Capella GUI reported as a non-blocking warning |
| `dev` topology | PASS | Exact CLI command started Uvicorn and Vite; both endpoints responded and `/api/health` returned 200 with 24 requirements |
| Canonical validation | PASS | 5 managed documents, 24 requirements, 24 unique UIDs, 24 unique MIDs and valid trace-link YAML |
| Ruff format/check | PASS | `backend` and `tools` |
| mypy strict | PASS | 34 backend source files |
| Backend pytest + coverage | PASS | 124 passed, 1 skipped; total coverage 87.50%, above the 85% gate |
| Prettier | PASS | frontend formatting check |
| ESLint | PASS | zero warnings under `--max-warnings 0` |
| Frontend Vitest + coverage | PASS | 27 tests passed; 87.77% coverage, above the 75% gate |
| Playwright fixture E2E | PASS | 1 scenario passed against an isolated disposable project |
| TypeScript/Vite production build | PASS | strict TypeScript compilation and production bundle |
| npm dependency audit | PASS | 0 known vulnerabilities reported |
| Native StrictDoc exports | PASS | HTML, PDF, Excel, JSON and ReqIF generated by StrictDoc 0.29.0 |
| Combined HTML | PASS | self-contained HTML with inline fixture diagrams |
| ReqIF structural verification | PASS | 5 `SPECIFICATION`, 24 `SPEC-HIERARCHY`, 24 `SPEC-OBJECT`, 27 `SPEC-RELATION`, 0 unresolved references |
| Fixture Capella index | PASS | 37 elements, 41 relations and 3 SVG diagrams; explicitly labelled fixture |
| Real Capella / GUI | NOT EXECUTED | No legal real model, Capella GUI or Requirements Viewpoint was available |

The one backend skip is the legally constrained live-model test and reports the
exact status `REAL CAPELLA TEST: NOT EXECUTED`; it is not counted as a fixture
pass.

## Reproduction commands

The repository-local commands are:

```bash
.venv/bin/python tools/reqpilot.py doctor
.venv/bin/python tools/reqpilot.py dev
.venv/bin/python tools/reqpilot.py validate
.venv/bin/ruff format --check backend tools
.venv/bin/ruff check backend tools
PYTHONPATH=backend .venv/bin/mypy backend/reqpilot
PYTHONPATH=backend .venv/bin/pytest backend/tests \
  --cov=reqpilot --cov-report=term-missing \
  --cov-report=xml:evidence/backend-coverage.xml --cov-fail-under=85
npm --prefix frontend run format
npm --prefix frontend run lint
npm --prefix frontend run test:coverage
REQPILOT_PYTHON=.venv/bin/python npm --prefix frontend run test:e2e
npm --prefix frontend run build
npm --prefix frontend audit
.venv/bin/python tools/reqpilot.py export --formats html excel json reqif
REQPILOT_CHROMEDRIVER=/absolute/path/to/chromedriver \
  .venv/bin/python tools/reqpilot.py export --formats pdf
.venv/bin/python tools/reqpilot.py index-capella
.venv/bin/python tools/verify_reqif.py exports/samples/strictdoc-requirements.reqif
```

On the verification host the Python environments and frontend dependencies were
mirrored under `/private/tmp` because macOS File Provider could evict executable
files in the workspace. This changes no command semantics or canonical source.
The exact `dev` command was run in that disposable mirror with the validated
loopback port set to `18081` because another local service occupied `8080`;
Uvicorn and Vite started, the frontend returned its document, and the backend
health endpoint returned HTTP 200. The command now derives the backend host and
port and the Vite proxy target from validated `project.yaml`.

## Backend and write-path coverage

The 124 passing backend tests cover native StrictDoc normalization, UID/MID
uniqueness, domain validation, internal relations, CRUD, Unicode/multiline
round-trips, optimistic concurrency, rollback, deleted-UID tombstones, path and
symlink confinement, YAML trace-link persistence, graph traversal and cycle
protection, matrices, impact paths, fixture rename/delete behavior, exports,
API errors and subprocess argument safety.

The Cobertura artifact is `evidence/backend-coverage.xml`. Its raw counters are
2,851 of 3,152 lines and 635 of 832 branches; coverage.py's combined total is
87.50%.

The write integration tests operate on disposable project copies. A candidate
is parsed and emitted through StrictDoc's model API, validated by a native
StrictDoc export, installed with `os.replace`, and re-read. Tested updates keep
MID stable; injected failures restore the original bytes. The compatibility
spike additionally proves that the canonical source manifest is unchanged; see
`evidence/strictdoc-spike.md`.

The Playwright test also uses a temporary copy. It updates `SYS-002`, observes a
new revision, creates a `satisfied_by` link, checks the graph and
requirement-to-function matrix, and generates the combined report. It does not
mutate the canonical checkout.

The FastAPI OpenAPI document contains 38 implemented operations. Five real
browser screenshots are stored as 1280×720 PNG files. The architecture and
requirements three-pane layouts were rechecked at that minimum width; the
document width remains 1280 px with no horizontal overflow.

## Native outputs

The successful native jobs retained in this checkout are:

| Format | Job ID | Main output |
|---|---|---|
| HTML | `72d93ff360ec4d2d915b1a38ace4583c` | `exports/strictdoc/html/72d93ff360ec4d2d915b1a38ace4583c/html/index.html` |
| PDF | `4d03fc4a8e664b79892f795990ca21f5` | `exports/strictdoc/pdf/4d03fc4a8e664b79892f795990ca21f5/html2pdf/pdf/bundle.pdf` |
| Excel | `358a76daebac401f97d5d1b3e7063f1e` | five workbooks under its `excel/` directory |
| JSON | `4961543d5cb34cc98dd78230b0408f08` | `exports/strictdoc/json/4961543d5cb34cc98dd78230b0408f08/json/index.json` |
| ReqIF | `0a3cb6013c704147995aee3a396a0f11` | `exports/strictdoc/reqif/0a3cb6013c704147995aee3a396a0f11/reqif/output.reqif` |
| Combined | n/a | `exports/combined/reqpilot-combined.html` |

Curated copies and their byte sizes/SHA-256 digests are recorded in
`exports/samples/manifest.json`. The manifest ties them to canonical revision
`67549a0c555d1375de4288dc20cc6dfa707315926fe199c21ce1ddf88cfac127`.

The ReqIF verifier parsed the native XML and found 5 specifications, 24 hierarchy
nodes, 24 objects, 27 relations, 199 identified elements, 531 references and no
unresolved reference. Every requirement object identifier is derived from its
stable StrictDoc MID; no custom ReqIF serializer is present.

The 41-page PDF bundle and five individual PDFs were rendered for visual
inspection with Chrome/PDFium. All five Excel workbooks were opened and checked
for expected sheet/table ranges and formula errors. The Poppler-specific warning
is retained in `evidence/known-limitations.md` rather than hidden.

## Capella boundary

The isolated Capella worker imports exact `capellambse==0.8.0`. In fixture mode,
indexing, UUID resolution, relationships, rename stability, broken-link
detection and three SVG routes are automated and pass over 37 elements and 41
relations. The fixture banner is covered by frontend tests.

No Capella GUI, real model or `.aird` was opened. No live SVG, before/after model
SHA-256, Requirements Viewpoint import, `.bridgetraces`, or Diff/Merge result is
claimed here. Their status remains:

```text
REAL CAPELLA TEST: NOT EXECUTED
```
