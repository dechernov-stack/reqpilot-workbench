# Python4Capella integration

Status: **P1 — NOT IMPLEMENTED**.

The P0 web backend never launches Python4Capella and never writes a Capella
model. If P1 is authorized after P0 acceptance, this directory may contain only
the reviewed Capella-side operation consumer described by the task.

